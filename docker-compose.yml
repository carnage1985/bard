services:
  bard:
    image: carnages/bard:latest
    container_name: bard
    restart: unless-stopped
    network_mode: host
    environment:
      DISCORD_TOKEN: "TOKEN"
      HOST_NETWORK: "true"
    volumes:
      - bard_data:/data
      - /hosting:/hosting:ro

volumes:
  bard_data:
